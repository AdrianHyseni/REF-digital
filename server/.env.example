DATABASE_URL="postgresql://postgres:postgres@localhost:5432/refnetwork?schema=public"
PORT=4000
# Comma-separated allowlist of origins for the web and admin apps. Leave unset
# to allow any origin (fine for local dev; Bearer-token auth means no CSRF risk).
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174
